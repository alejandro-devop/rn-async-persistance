import React from 'react';
/**
 * Loads async storage and data and initializes the redux store
 * @param param0
 * @returns
 */
declare const ReduxSessionProvider: React.FC<{
    children?: React.ReactNode;
}>;
export default ReduxSessionProvider;
